spring:
  data:
    mongodb:
      uri: mongodb://common:passwd@ip:port/commonDB
  thymeleaf:
    mode: LEGACYHTML5
#    cache: false
    prefix: classpath:/templates/
    suffix: .html
    servlet:
      content-type: text/html
  mvc:
    static-path-pattern: /static/**
    view:
#      prefix: /admin
      suffix: .html
  resources:
    static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/


me:
  mongoAddress: ip:port